const selectionSort = (list) => {
    
    //1. Take the full array iterate over it to find the lowest number
    //2. Swap it with the first one
    //3. Start step 1 but from the second index the array
    //4. Repeat
    
}

const list = [4,9,7,1,3,6,5];

